@model Febris.ModelLibrary.ViewModels.LineChart
<!--<div class="dashboard_graph">

    <div class="row x_title">
        <div class="col-md-6">
            <h3>Network Activities <small>Graph title sub-title</small></h3>
        </div>
        <div class="col-md-6">
            <div id="reportrange" class="pull-right" style="background: #fff; cursor: pointer; padding: 5px 10px; border: 1px solid #ccc">
                <i class="fa fa-calendar"></i>
                <span>@DateTime.Now.AddDays(-7).ToLongDateString() -  @DateTime.Now.ToLongDateString()</span> <b class="caret"></b>
            </div>
        </div>
    </div>

    <div class="col-md-12 col-sm-12" id="Division4">
        <div class="spinner-border" role="status">
            <span class="visually-hidden"></span>
        </div>-->
@*<div id="chart_plot_01" class="demo-placeholder"></div>*@
<!--</div>

</div>-->
@*<div class="col-md-8 col-sm-8  ">*@
<div class="x_panel">
    <div class="x_title">
        <h2>@Model.Title<small>@Model.Description</small></h2>
        <ul class="nav navbar-right panel_toolbox">
            @*<li>
                <a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
            </li>*@
            @*<li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="fa fa-wrench"></i></a>
                <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                    <a class="dropdown-item" href="#">Settings 1</a>
                    <a class="dropdown-item" href="#">Settings 2</a>
                </div>
            </li>*@
            <li>
                <a class="close-link"><i class="fa fa-close"></i></a>
            </li>
        </ul>
        <div class="clearfix"></div>
    </div>
    <div class="x_content">
        <canvas id="@Model.IdToUse"></canvas>
    </div>
</div>
@*</div>*@
<script>

    var chartdata = @Html.Raw(Json.Serialize(Model.ChartEntryList));
    var labels = [];
    var data = [];
    $.each(chartdata, function (index, value) {
        labels.push(value.label);
        data.push(value.quantity);
    });
    var ctx = document.getElementById(@Html.Raw(Json.Serialize(Model.IdToUse))).getContext('2d');

    var chart = new Chart(ctx, {
        type: 'line',
        data: {
            labels: labels,
            datasets: [{
                data: data,
                fill: true,
                borderColor: 'rgb(75, 192, 192)',
                backgroundColor: 'rgba(75, 192, 192, 0.2)',
                tension: 0.1
            }]
        }
    });



    @*var chart = new Chart(ctx, {
        type: 'line',
        data: {
            labels: labels,
            datasets: [{
                //label: @Model.Title,
                //backgroundColor: 'rgb(255, 99, 132)',
                //borderColor: 'rgb(255, 99, 132)',
                data: data,
                backgroundColor: [
                    "#28a745",
                    "#6c757d",
                    "#6f42c1",
                    "#343a40",
                    "#20c997",
                    "#007bff",
                    "#6610f2",
                    "#fd7e14",
                ]

            }]
        }

    });*@



</script>
<script src="~/js/site.js"></script>